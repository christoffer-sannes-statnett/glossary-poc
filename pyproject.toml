[project]
name = "glossary-poc"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "rdflib>=7.6.0",
]

[dependency-groups]
dev = [
    "mypy>=1.0",
    "pre-commit>=4.0",
]

[project.scripts]
generate = "scripts.generate:main"
